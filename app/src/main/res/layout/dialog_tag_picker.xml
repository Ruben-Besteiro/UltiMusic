<?xml version="1.0" encoding="utf-8"?>
<!-- Buscador para añadir una etiqueta a una canción (ver TagPickerDialogFragment): campo de texto
     arriba (filtra en tiempo real, sin tildes/mayúsculas) + lista debajo. Al principio, sin texto,
     salen todas las etiquetas asignables. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="24dp"
    android:paddingTop="8dp"
    android:paddingBottom="16dp">

    <EditText
        android:id="@+id/tagSearchInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_search_bar"
        android:hint="@string/tag_picker_search_hint"
        android:imeOptions="actionSearch"
        android:importantForAutofill="no"
        android:inputType="text"
        android:maxLines="1"
        android:paddingHorizontal="14dp"
        android:paddingVertical="10dp"
        android:textColor="@color/um_on_background"
        android:textColorHint="@color/um_on_surface_muted"
        android:textSize="15sp" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/tagPickerList"
        android:layout_width="match_parent"
        android:layout_height="320dp"
        android:layout_marginTop="12dp"
        android:clipToPadding="false" />

</LinearLayout>
