<?xml version="1.0" encoding="utf-8"?>
<!-- Etiquetas de una canción (ver SongTagsDialogFragment): un ScrollView con una fila por etiqueta
     (item_tag_removable.xml, inflada por código igual que AddToPlaylistDialogFragment con sus
     casillas), un aviso si no tiene ninguna, y FUERA del ScrollView (siempre visible aunque haya
     muchas) el botón "+ Añadir" que abre TagPickerDialogFragment. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="24dp"
    android:paddingTop="8dp"
    android:paddingBottom="16dp">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxHeight="320dp">

        <LinearLayout
            android:id="@+id/songTagsContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />

    </ScrollView>

    <TextView
        android:id="@+id/songTagsEmpty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:text="@string/song_tags_empty"
        android:textColor="@color/um_on_surface_muted"
        android:textSize="14sp"
        android:visibility="gone"
        tools:visibility="visible" />

    <TextView
        android:id="@+id/btnAddTag"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:background="@drawable/bg_add_tag_button"
        android:clickable="true"
        android:focusable="true"
        android:foreground="?attr/selectableItemBackground"
        android:gravity="center"
        android:paddingVertical="12dp"
        android:text="@string/add_tag_button"
        android:textColor="@color/um_on_background"
        android:textStyle="bold" />

</LinearLayout>
