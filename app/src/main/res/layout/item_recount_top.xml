<?xml version="1.0" encoding="utf-8"?>
<!-- Fila de cualquiera de los tres tops de UltiMusic Recount (canciones, artistas, géneros): la
     misma para los tres, ver RecountTopAdapter.

     El puesto va delante con ancho fijo para que los nombres queden alineados en columna aunque el
     top llegue al 10. La imagen se oculta en el top de géneros (un género no tiene carátula, ver
     GenreSummary) y en la fila de canciones borradas, y entonces el nombre se pega al número.

     No tiene `selectableItemBackground`: aquí no se puede pinchar nada. Es una foto de lo que se
     escuchó, no un atajo para reproducirlo. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingVertical="6dp">

    <TextView
        android:id="@+id/recountRank"
        android:layout_width="28dp"
        android:layout_height="wrap_content"
        android:gravity="end"
        android:textColor="@color/um_on_surface_muted"
        android:textSize="16sp"
        android:textStyle="bold"
        tools:text="1" />

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/recountCover"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:layout_marginStart="12dp"
        android:background="@drawable/cover_placeholder"
        android:contentDescription="@string/song_cover_desc"
        android:scaleType="centerCrop" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/recountLabel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/um_on_background"
            android:textSize="16sp"
            android:textStyle="italic"
            tools:text="I Want To Break Free" />

        <TextView
            android:id="@+id/recountStats"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/um_on_surface_muted"
            android:textSize="13sp"
            tools:text="42 reproducciones | 2:51:20 | 18%" />

    </LinearLayout>

</LinearLayout>
