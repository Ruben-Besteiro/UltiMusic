<?xml version="1.0" encoding="utf-8"?>
<!--
  Contenido de los ajustes del reproductor de vídeo (ver
  IPodDialogFragment.showVideoSettingsDialog). Se infla dentro de un AlertDialog corriente con
  setView, así que no lleva fondo ni toolbar propios: por eso no fija colores de texto, usa los del
  tema del diálogo, igual que el resto de AlertDialog de la aplicación (confirmar borrado, nombre de
  playlist nueva...).
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingTop="8dp"
    android:paddingEnd="24dp"
    android:paddingBottom="8dp">

    <!-- Mismo campo que el editor de metadatos: no se escribe a mano, se toca y abre el buscador de
         YouTube (ver [IPodDialogFragment.showVideoSettingsDialog]). -->
    <com.google.android.material.textfield.TextInputLayout
        style="@style/Widget.UltiMusic.TextField"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/field_video_url"
        app:helperText="@string/field_video_url_hint">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/inputVideoUrl"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:focusable="false"
            android:focusableInTouchMode="false"
            android:cursorVisible="false"
            android:longClickable="false"
            android:maxLines="1" />
    </com.google.android.material.textfield.TextInputLayout>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="@string/video_settings_offset_title"
        android:textSize="16sp"
        android:textStyle="bold" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="@string/video_settings_offset_subtitle"
        android:textSize="13sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <com.untar.ultimusic.ui.common.ValueRuler
            android:id="@+id/offsetRuler"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:contentDescription="@string/video_settings_offset_title" />

        <EditText
            android:id="@+id/offsetValue"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:background="@null"
            android:gravity="end"
            android:imeOptions="actionDone"
            android:importantForAutofill="no"
            android:inputType="numberSigned"
            android:maxLength="6"
            android:maxLines="1"
            android:minWidth="72dp"
            android:textSize="18sp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="4dp"
            android:text="@string/video_settings_offset_unit" />

    </LinearLayout>

</LinearLayout>
