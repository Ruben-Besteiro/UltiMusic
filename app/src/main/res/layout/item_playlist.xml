<?xml version="1.0" encoding="utf-8"?>
<!-- Fila de la lista de Listas: como la de Artistas/Productores pero SIN imagen. Nombre +
     recuento/duración + botón de 3 puntos (renombrar / borrar). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingHorizontal="16dp"
    android:paddingVertical="8dp">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/playlistName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/um_on_background"
            android:textSize="16sp"
            android:textStyle="italic"
            tools:text="Favoritas" />

        <TextView
            android:id="@+id/playlistSubtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/um_on_surface_muted"
            android:textSize="14sp"
            tools:text="12 canciones | 48:03" />

        <!--
          Cuántas canciones de la lista ya sonaron esta vuelta del ciclo "sin repeticiones" (ver
          PlaylistHistoryStore), con un botón de reinicio justo al lado del texto (por eso van juntos
          en un LinearLayout horizontal `wrap_content`, no estirados a todo el ancho de la fila).
          Oculta por defecto: solo aparece mientras el historial tenga algo dentro.
        -->
        <LinearLayout
            android:id="@+id/playlistHistoryRow"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:visibility="gone"
            tools:visibility="visible">

            <TextView
                android:id="@+id/playlistHistoryText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/um_on_surface_muted"
                android:textSize="13sp"
                tools:text="8 reproducidas" />

            <ImageButton
                android:id="@+id/btnResetPlaylistHistory"
                android:layout_width="22dp"
                android:layout_height="22dp"
                android:layout_marginStart="6dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/playlist_reset_history_desc"
                android:padding="2dp"
                android:src="@drawable/ic_restart"
                android:tint="@color/um_on_surface_muted" />

        </LinearLayout>

    </LinearLayout>

    <ImageButton
        android:id="@+id/btnPlaylistMore"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginEnd="5dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/playlist_more"
        android:src="@drawable/ic_more_vert" />

</LinearLayout>
